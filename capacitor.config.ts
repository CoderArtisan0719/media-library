import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourdomain.medialibrary',
  appName: 'Media Library',
  webDir: 'frontend/build',
  bundledWebRuntime: false
};

export default config;
